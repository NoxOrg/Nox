#
# generator.nox.yaml
#
# This file configures which layers of code the Nox generator 
# will generate.
#

# Genrates momain models, events and related types
domain: true

# Generates application logic and events
application: true

# Generates persistence, DbContext and model configuration and mapping
infrastructure: true

# Generates APi's and UI's
presentation: true
