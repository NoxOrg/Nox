@using Nox.Ui.Blazor.Lib.Components.Generic
@using Nox.Ui.Blazor.Lib.Services
@using Cryptocash.Ui.Forms.Add
@using Cryptocash.Ui.DataGrid

@namespace Cryptocash.Ui.Pages

@page "/Bookings"

@inject ApplicationState AppState
@{
    AppState.PageTitle = "Bookings";
}

<MudText>Bookings</MudText>

<MudGrid Spacing="0">
    <MudItem xs="12">
        <MudText>Search section</MudText>
    </MudItem>
    <MudItem xs="12">
        <Actions OnAdd="ShowAddDialog" />
    </MudItem>
    <AddBookingForm @bind-IsVisible="showAddDialog" OnSubmit="AddBookingSubmit" OnCancel="HideAddDialog" />
    <MudItem xs="12">
        <BookingsDataGrid />
    </MudItem>
</MudGrid>

@code{
#region ADD
    private bool showAddDialog = false;

    private void ShowAddDialog()
    {
        showAddDialog = true;
    }

    private void HideAddDialog()
    {
        showAddDialog = false;
    }

    private void AddBookingSubmit()
    {
        showAddDialog = false;
    }
#endregion ADD
}