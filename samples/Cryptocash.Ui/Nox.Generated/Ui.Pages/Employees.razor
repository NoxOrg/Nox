@using Nox.Ui.Blazor.Lib.Components.Generic
@using Nox.Ui.Blazor.Lib.Services
@using Cryptocash.Ui.Forms.Add

@namespace Cryptocash.Ui.Pages

@page "/Employees"

@inject GlobalDataService GlobalData
@{
    GlobalData.PageTitle = "Employees";
}

<MudText>Employees</MudText>

<MudGrid Spacing="0">
    <MudItem xs="12">
        <MudText>Search section</MudText>
    </MudItem>
    <MudItem xs="12">
        <Actions OnAdd="ShowAddDialog" />
    </MudItem>
    <AddEmployeeForm @bind-IsVisible="showAddDialog" OnSubmit="AddEmployeeSubmit" OnCancel="HideAddDialog" />
</MudGrid>

@code{
#region ADD
    private bool showAddDialog = false;

    private void ShowAddDialog()
    {
        showAddDialog = true;
    }

    private void HideAddDialog()
    {
        showAddDialog = false;
    }

    private void AddEmployeeSubmit()
    {
        showAddDialog = false;
    }
#endregion ADD
}